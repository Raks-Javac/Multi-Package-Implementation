library b;

import 'package:flutter/material.dart';

class HelloB extends StatelessWidget {
  const HelloB({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
